<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cookshop">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SynchronizeActivity"></activity>
        <activity android:name=".view.articleViewUpdateAdd.EditArticle" />
        <activity android:name=".view.articleViewUpdateAdd.ArticleViewer" />
        <activity
            android:name=".view.articleViewUpdateAdd.AddArticleActivity"
            android:label="@string/add_article"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".view.main.MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>